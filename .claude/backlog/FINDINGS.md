# Findings — parked, not yet placed

**One or two lines each, dated.** A buffer, not a second backlog: it holds findings whose home is
**not local and not yet decided** — a possible row, a suspected skill or convention problem, a cost
pattern nobody has named yet.

**If a finding's home is obvious, write it there instead and do not park it.** A mechanism goes in a
comment beside the code, a rule goes in a test that fails, a unit of work goes to `queue` as a row.
Parking those is how a session ends with nothing written down *and* a growing file.

Format: `- YYYY-MM-DD — **what happened.** why it might matter (pointer: file, item id)`

**The date goes outside the bold, and this is load-bearing.** Every sweeper and `./next --findings`
find entries by line shape, so an entry whose date sits inside the `**` is skipped and nobody ever
notices — two such entries once made `MEASUREMENT.md` publish 26 findings in one sentence and 28 two
paragraphs later. Readers match `^- (\*\*)?20[0-9]{2}-` to tolerate the drift; writers use the
canonical form so as not to add to it. **Entry order is not guaranteed** — sessions have appended at
both ends — so a sweep reads to the end rather than stopping at the first entry outside its window.

**Emptying this file is `queue`'s and `retro`'s job and their skills carry the rules**: who takes
which entries, what is expired unprocessed, and why a sweeper removes only what it processed. The
normal state of this file is empty, and **if it has grown, that is itself the finding**.

---

- 2026-09-05 — **`queue` Step 0's byte-comparison drift check reports a false positive on `item.md` every time.** The step says to check "every executable in this skill's `templates/`, plus `item.md`" against the backlog's copies, but `item.md` is consumed at write time and has no installed copy — there is no `.claude/backlog/item.md` for any project — so a literal `diff` of the named file list reports DIFFERS on it in every backlog, forever. The four executables compared cleanly here; only `item.md` was noise. Either the step names `item.md` as compared against the *last-written item's* shape, or it says outright that `item.md` has no installed counterpart and is excluded from the byte comparison (pointer: `skills/queue/SKILL.md` Step 0, "Compare the bytes, not a version string").
- 2026-09-06 — **The mutation-sweep rule enumerates decision sites, and a decision's *content* is computed one layer below them.** `develop`/`verify` and `testing-conventions.md` now say to build the list from "every call site of the function that decides" plus every NOTE-and-continue. On 0038 that rule was followed exactly and still missed all thirteen branches of `takeable_develop`, `gate_from` and `depth_stopper` — the helpers that produce `--drive`'s DEPTH line, which is AC29's entire subject. Eight were free to delete with the suite green, including one that let AC29's own fixture pass because it asserted the stopper's **id** while the deleted arm changed only its **reason**. The general form that would have caught them is about output, not control flow: *every branch whose deletion can change any character the mode prints is a site, whether or not it decides anything* — enumerate backwards from the printed line rather than outwards from the ladder (pointer: `testing-conventions.md` line 16, item 0038 *Re-entry 2026-09-06 (third)*).
- 2026-09-06 — **A mutation-sweep driver that asserts before it restores poisons the next run's baseline, and only the control mutation catches it.** A driver that validated the mutation site before the restore step left a mutation in the working tree when that validation failed mid-run; the next run copied the mutated file as its baseline, and every reading after that was measured against the wrong file. It surfaced only because the no-op control diffed clean and reported "mutation did not land". Two additions to the sweep rule: restore in a `finally` rather than on the happy path, and the control's job includes catching a **baseline that already contains a mutation**, not only an unstable harness — which the conventions currently do not say (pointer: `testing-conventions.md` line 16, "Run one no-op mutation as a control").
- 2026-09-06 — **An `assert_contains` on a flag name is matched by the prose that explains the flag, not only by the line that documents it.** 0038's AC11 asserts `./next --help` lists both new modes as `assert_contains "$out" '--drive'`. `usage()` can drop `--drive` from its synopsis *and* delete its whole five-line description block — leaving a help text in which the mode is undiscoverable — and the suite stays at `200 passed, 0 failed`, because a later paragraph explaining the exit codes happens to say `--drive`. Its twin `--findings` reds under the same mutation only because it appears nowhere else. A help assertion has to pin the mode's **own line** (the synopsis entry, or `^  ./next --drive`), never the bare flag; a flag name is the single most repeated token in the text it is being sought in. Generalises past `next`: every `--help` guard in `tests/` is written this way (pointer: `tests/next.test.sh:763`, item 0038).
- 2026-09-06 — **A mutation-sweep rule scoped to one mode leaves the other mode's branches unenumerated, and the scoping is invisible because the rule reads as general.** 0038 ships two modes. Three passes extended the enumeration rule a layer at a time and the fourth statement of it opens "For `--drive` that is the two ladders *and* every helper…" — correct, complete for `--drive`, and silent about `--findings`. Sweeping both modes on this pass, every `--drive` site was red and three `--findings` sites were free to delete: the preamble skip that stops the count inflating, the at-or-over-threshold half of the report line (`grep "at or over" tests/` is empty), and the absent-`FINDINGS.md` early return, whose deletion turns a clean `0 entries; threshold 8` at rc 0 into `awk: can't open file` at rc 2 on exactly the unmigrated install the Compatibility NFR is about. The rule should be scoped to *the change*, not to the mode whose defect prompted it (pointer: `testing-conventions.md` line 16, item 0038).
- 2026-09-06 — **A file you mutate transiently and restore is inside your file scope, and no step says to declare it.** `develop` Step 1 defines `touches:` as "what you will actually open" and adds a rule for a file you will *create*; a file you will break on purpose and put back fits neither reading, since the committed diff never shows it. But it is the sharpest kind of hold: proving a guard red means the suite is deliberately red for the seconds the mutation is live, and a concurrent session running the whole suite in that window collects reds that belong to nobody, cannot be reproduced a moment later, and point at a file its own ticket never touched — `develop` Step 5's throwaway-worktree comparison would clear it, at the cost of the whole diagnosis. Step 5 already tells the *arriving* session to check `pgrep` for a live run before starting one and says nothing to the session holding the mutation. Declared it in `touches:` on 0038 with an inline reason, which reads correctly but is invention rather than instruction (pointer: `skills/develop/SKILL.md` Step 1 `touches:`, and Step 5's "a whole-suite run is a shared resource"; item 0038).
- 2026-09-06 — **A change that *wraps* an existing branch takes on that branch's evidence, and a sweep scoped to "the diff" still reads it as somebody else's code.** 0038 added `if [ "${1:-}" != "--drive" ]` around `next`'s pre-existing fixed-shape refusal so the new mode skips it. The wrapper is pinned in both directions — inverting it and deleting it each red AC28's reordered-table fixture — but the condition *inside* it, `[ "$shape" = "ID/Title/Next/Status/Parent" ]`, can be replaced with `[ 1 ]` and all 21 suites stay green: `grep -rn 'unexpected table shape' tests/` is empty, so the pre-existing modes' refusal to parse a table they cannot read has never had a guard. That is the one pre-existing exit code AC11's compatibility clause names and its fixture does not reach. The last pass's rule was "scope the sweep to the change, not to the mode whose defect prompted it"; the refinement is that a change's surface includes every branch it can now switch off, not only the lines it adds — a wrapper inherits the responsibility for what it wraps (pointer: `testing-conventions.md` line 16, `skills/queue/templates/next` shape check, item 0038).
- 2026-09-06 — **A dated "confirmed by running it" about an external tool ages exactly like a quoted figure, and three of them failed on the same command.** 0039's FR13 said a stage is launched with `claude -p --json-schema <that file>` and cited a 2026-08-24 run as confirmation. The flag does not take a path — it fails with `--json-schema is not valid JSON: JSON Parse error: Unrecognized token '/'` — and the inline `"$(cat f.json)"` form is what works. Two more surfaced in the same three-second probe, neither visible in any prose: **a nested `claude -p` must redirect stdin** or it waits and then prints `Warning: no stdin data received in 3s` *into the stream being parsed as JSON*, so a good subprocess reads as a schema failure; and **`--max-budget-usd` below a session's ~20k-token startup floor returns `Error: Exceeded USD budget` rather than the result** (USD 0.05 failed, USD 0.25 passed), which reads identically to a broken CLI. `develop` Step 2 already says a quoted *figure* is a cache to re-read; the generalisation is that **any claim a ticket makes about an external tool's interface is a cache too**, and re-running it costs one command. Worth stating in that step alongside the figure rule (pointer: `skills/develop/SKILL.md` Step 2, "a figure a ticket quotes about a file it does not own"; item 0039).
- 2026-09-06 — **A QA plan can specify a guard that cannot be written as stated, and the plan is not the authority on that.** 0039's QA plan asked for "a grep asserting `--bare` appears nowhere in `skills/orchestrate/`". The skill has to *explain* why `--bare` is disqualifying, so the guard as specified reds exactly the file that documents the rule best — the negative-assertion trap `testing-conventions.md` already names, arriving from a ticket rather than from an author. Written instead as: the flag must appear in no **fenced code block** (where an invocation lives), with the prose checked separately and positively. Nothing in `develop` tells a session that a QA plan's *check* can be wrong in the way it tells it an FR can be stale, so the correction has no home but the item's notes — where the next QA pass will read the plan, not the note (pointer: `skills/develop/SKILL.md` Step 2's staleness rules, which cover FRs and ACs but not the QA plan's named checks; item 0039).
- 2026-09-06 — **The enumerated-subjects defect recurred in this repo's own guards, in the two files a new skill most needs to be under.** `tests/last-line.test.sh` and `tests/reporting.test.sh` both carried `SKILLS="design develop prototype queue retro verify"`, so adding a seventh skill put it in the set those rules govern and outside the set they check — green by construction on exactly the change that most needed checking. `tests/skill-size.test.sh` and `tests/money-in-skill-prose.test.sh` derive theirs and were correct with no edit. Both are now derived, taking the tree root as a parameter so the fixture cases keep authored trees. The rule is in `testing-conventions.md` and in `money-in-skill-prose.test.sh`'s own header, and was still not followed — findability is not adherence. **A sweep for the remaining instances is the actionable half**: `grep -n '^[A-Z_]*=\"[a-z ]*\"' tests/*.test.sh` finds hardcoded subject lists, and nobody has run it (pointer: `testing-conventions.md`, "a guard that enumerates its subjects cannot notice a new one"; item 0039).
- 2026-09-06 — **A presence grep is anchored to the file, not to the place, whenever its phrase occurs more than once — and that is mechanically checkable before the mutation sweep runs.** 0039's build session swept its own guards, found three that could not fail, fixed them, and wrote the shape down. Three more of the identical shape survived to the QA pass, and what separates them from the three that were caught is not subtlety: all three missed ones are `grep -qF '<phrase>' "$FILE"` where the phrase also appears elsewhere in the same file for an unrelated reason. AC19's `--max-budget-usd` is satisfied by the Step 1 *probe*'s cap, so deleting the cap from the Step 3 *dispatch* is green; AC21's `findings-parked count` is satisfied by Step 4's schema description, so deleting it from Step 8's report step is green; AC16's `Claim tokens` is satisfied by Step 7's unrelated citation, so deleting Step 5's orphaned-claim rule is green. The build session had even fixed exactly this for `< /dev/null` one loop above the AC19 check. **The cheap general rule is a count, not a judgement:** before a guard asserts a phrase is present, `grep -c` it — more than one hit means the guard cannot distinguish the place the criterion names from anywhere else, and it has to be scoped (extract the section first, as this same file's AC20 guard does) or anchored to order (as its AC27 guard does). That is runnable over every existing guard today: `grep -oE "grep -q[F]? .[^\"']+. \"\\\$(SKILL|README|ROOT)" tests/*.test.sh` lists the candidates. `testing-conventions.md` says a check must be able to fail; it does not say that a *unique-looking phrase* is the thing most likely to make it unable to (pointer: `testing-conventions.md` line 16, and `skills/verify/SKILL.md` Step 3's "adjacent measurement" paragraph; item 0039).
- 2026-09-06 — **A ticket whose first FR is a gate that can kill the rest of the ticket has no shape in `item.md`, and the template quietly assumes every FR ships.** 0104's FR1 is a measurement with written kill criteria; FR2-FR6 exist only if it clears, and the honest close on a kill is "MEASUREMENT.md records the negative result and nothing else changed". Nothing in the template or in `queue` Step 2 provides for a conditional FR, so the condition had to be written inline into each dependent FR ("and only if FR1 clears") and a whole AC spent asserting the *killed* state (AC2 — verify unchanged) so that `verify` cannot read a kill as an unfinished ticket. `discovery-conventions.md` has the kill-criteria discipline and the item template has no field for its result, which means every gated ticket re-invents this and a later reader cannot tell a killed FR from a skipped one. Either the template names a conditional-FR form, or `queue` says a gate belongs in its own ticket with the dependents `blocked_by` it (pointer: `skills/queue/templates/item.md` *Functional requirements*, `skills/queue/SKILL.md` Step 2; the ticket was 0104, withdrawn before any work started and preserved at commit `7764732`).
- 2026-09-06 — **The `grep -c` check proposed for finding unfalsifiable presence guards misses two of the three defects that prompted it, and one blind spot is self-defeating.** The entry above proposes, as the cheap general rule, counting a phrase's hits before trusting a presence guard: more than one means the guard cannot tell the criterion's place from anywhere else. Run against 0039's pre-fix guard file it returned **AC21 only**. **(1) AC16 is invisible to it because the duplicate straddles a line break.** `Claim tokens` appears twice in the skill, but Step 5's instance wraps as `*Claim` / `tokens*`, so `grep -cF 'Claim tokens'` answers **1** and the guard reads as safely unique — the counter is line-based, which is the exact blindness it was proposed to detect, so it under-counts precisely the phrases most likely to be duplicated in flowing prose. **(2) AC19 is invisible because it is not syntactically a whole-file grep**: it greps a *filtered stream*, `fenced "$SKILL" | grep -qF -- "$want"`, which is semantically whole-file over every fenced block concatenated but matches no pattern looking for `grep ... "$FILE"`. So the rule as written finds the easy third and leaves the two that needed a mutation to find. Two corrections, both cheap: **count on flattened text** (`tr '\n' ' ' | tr -s ' '` before counting, which is what 0039's new `section()` helper does before matching), and **treat any guard whose subject is a filtered stream as whole-file** — the filter is a scope only if it selects *one* block, and `fenced` selects all of them. The general form is that a phrase-uniqueness check inherits every weakness of the matcher it is built from, so it cannot be the only net; the mutation sweep is still what finds these, and this check is a pre-filter that is now known to leak (pointer: the 2026-09-06 presence-grep entry above, `tests/orchestrate.test.sh` `section()`; item 0039).
- 2026-09-06 — **This repo's first standalone `.py` shipped unhinted, and no guard in a 22-file suite could have said so.** `tools/validate-json-schema.py` (new in 0039) had no type hints on any of its three functions, against `CONVENTIONS_CORE.md`'s *"Python with full type hints"* — a principle, not a preference. It reached a QA pass and was caught only by a human-shaped read of the diff in `verify` Step 4's always-on convention pass, because no AC and no NFR row covered it and the suite greps prose rather than parsing code. Fixed for that file here; **the pre-existing embedded python in `tools/*.sh` is unhinted too and still needs a row**, as does a guard, which is the actionable half: `python3 -c` over `ast` can assert that every `def` in every tracked `.py` carries annotated args and a return type in about ten lines, and would hold the rule for the *next* new file rather than relying on a reviewer noticing. The general shape is that a convention which is a **principle** and is also mechanically checkable should have a guard rather than a reviewer, and this repo's suite currently enforces its prose conventions and none of its code ones (pointer: `CONVENTIONS_CORE.md` *Code*, "Use types"; `tools/validate-json-schema.py`; item 0039).
- 2026-09-07 — **A fixture guard that matches a `case` pattern against whole captured output is start-anchored, and reads as green while the behaviour is half broken.** 0039's AC16 fixture drives the real `./next` over a backlog holding an orphaned claim and asserts the held row is not offered, via `case "$out" in "TAKE      0001"*) bad ...`. That matches only when `TAKE      0001` is the **first thing printed**. Proving the guard falsifiable took two attempts and the first is the finding: letting `in-progress` through the status filter was not enough, because `./next` refuses the row a second time via the `touches:` collision check — output came back as `COLLIDES 0001 ... / TAKE 0002 ...`, so a reader with one of its two defences removed produced a green guard. Only breaking **both** put `TAKE      0001` at position zero and reddened it. Two general shapes: **(1)** a `case` over `"$out"` is a prefix match on a multi-line capture, not a line match, so any warning, `COLLIDES`, `SKIP` or `DRIFT` line printed ahead of the regression hides it — `printf '%s' "$out" | grep -q '^TAKE      0001'` is the same cost and says what was meant; **(2)** where a behaviour is defended by two independent mechanisms, a mutation that removes one proves nothing, and a guard verified by single-mutation is verified against the *stronger* of the two only. Worth a sweep for `case "$out" in` across `tests/` (pointer: `tests/orchestrate.test.sh` AC16 fixture, `.claude/backlog/next` status filter + `collision_report`; item 0039).
- 2026-09-07 — **`touches:` can name a file for an edit that never happens, and nothing anywhere notices.** 0039's re-entry declared `- .claude-plugin/plugin.json  # version bump — the install predates this change`; the version was never bumped and the re-entry diff touches two other files. `touches:` is a *reservation* — a claim on files so a concurrent session avoids them — so an unused reservation is invisible by construction: `./next --drift` compares Status against `blocked_by`, `./claim` and `./close` never diff the range, and no test compares a ticket's declared touches against what its commits actually changed. The asymmetry is the point: a touch that reaches **further** than declared is the dangerous one and is also unchecked, but a touch that reaches **less far** silently carries an intention that reads, to the next session, as a completed step — here, one that would have made the skill runnable. Cheap check with real value: at close or hand-off, diff the ticket's commit range against `touches:` and report both directions as a note rather than a gate, since over-declaring is legitimate and under-delivering is what wants saying out loud (pointer: `.claude/backlog/close`, `./next --drift`; item 0039).
- 2026-09-07 — **An NFR row has no falsifiability guard, and a one-line prose gap survived two QA passes that both scored it.** 0052 shipped `tests/falsifiable-acs.test.sh` so the lifecycle asks whether an *acceptance criterion* can be made to fail; nothing asks it of an *NFR row*, and no template step requires a row to name the check that would red it. 0039's Dependencies row asked two things — name the `claude` CLI dependency, say what happens without it — and the naming half was unmet, flagged ⚠️ on 2026-09-06, carried, then failed ❌ on 2026-09-07, the whole gap being one line of README the entire time. An AC that cannot fail is now caught upstream; an NFR that cannot fail is caught by whichever QA pass happens to read the row carefully, which is why this one cost two. Either the NFR table gains a "how it would red" column or `verify` Step 4's always-on pass asks the falsifiability question of the table too (pointer: `tests/falsifiable-acs.test.sh`, `skills/queue/templates/item.md` *Non-functional requirements*, `skills/verify/SKILL.md` Step 4; item 0039). This still needs a row.
- 2026-09-07 — **The `plugin.json` entry in that `touches:` is not merely unused, it is undischargeable by a develop session — which sharpens the entry above it rather than repeating it.** The prior entry reads the never-bumped version as a stale reservation. The stronger fact is that no develop session *could* have discharged it: `tools/release` derives the next version from the **remote's** `plugin.json` (0075, after a hand-picked version collided), so a hand-bump both re-introduces that defect and still needs the push approval the chain opens with. A version bump is therefore a release act, never a build act, and `touches:` naming it invites three sessions in a row to record not having done it. The fix is upstream of `touches:`: `queue` should not put a version bump in a ticket's expected file scope at all (pointer: `tools/release` steps 1-3, `skills/queue/templates/item.md` `expects:`; items 0039, 0075). This still needs a row.
- 2026-09-07 — **Ticket ID `0104` has now been issued to two different tickets and will be issued to a third, because withdrawing a ticket decrements `next_id`.** The first `0104` — *"Give every findings entry a terminal disposition"* — landed on 2026-09-05 at commit `b9a5ee0` and is cited by ID in two guards (`tests/retro-tool-edit.test.sh`'s four `0104 AC*` case labels, `tests/skill-size.test.sh`'s recorded over-goal justification for `skills/retro/SKILL.md`), yet its row is in neither `QUEUE.md` nor `DONE.md` and no `items/0104-*.md` exists. The ID was then reissued to *"verify in-window fix for test-side-only reds"*, withdrawn at `3b72d38`, which deleted that item **and reset `next_id` from 105 back to 104** — so the next `queue` ticket takes `0104` a third time, and both of those guard citations silently start pointing at it. `next_id` is documented as *monotonic* in `config.yml`'s own comment, and the withdrawal path breaks that invariant while looking like tidy-up. Two halves, both needing a row: **(1)** withdrawal must burn the ID, not recycle it — the counter is what makes an ID a stable identifier, and `CONVENTIONS_CORE.md` names stable identifiers as a decision that cannot be retrofitted once anything cites one; **(2)** a guard citing an item ID has no way to notice its referent changed underneath it, so `citations.test.sh`-style checking of item IDs against the union of `QUEUE.md`, `DONE.md` and `items/` would have reported the first `0104` as unresolvable the day it landed. Found while verifying `0076`, whose guard file carries four of the affected citations (pointer: `.claude/backlog/config.yml` `next_id`, commits `b9a5ee0` and `3b72d38`, `tests/retro-tool-edit.test.sh:118-146`, `tests/skill-size.test.sh:91`; items 0076, 0104). This still needs a row.
- 2026-09-07 — **`verify` Step 7's fresh-capture rule assumes a vanished dirty path is either committed or destroyed, and a third case leaves it unresolvable from the record.** Step 7 says a `git status --porcelain` printing nothing after a dirty Step 2 is "ambiguous between *they committed* and *I destroyed their uncommitted work*, and nothing but a fresh read separates those". Verifying `0076`, `skills/orchestrate/SKILL.md` was ` M` at Step 2 and clean at verdict time — and **neither** case was evidenced: no commit reachable from `HEAD` touched that path after `e3bc5c8` (already an ancestor at Step 2), `git stash list` was empty, and no branch or ref carried it. The most likely actual cause is the third case the rule does not name — **the owning session reverted its own edit, or moved it into its own worktree** (`git worktree list` showed a second session pinned at the same SHA) — and none of the three leaves a trace a later reader can distinguish. So the fresh read the rule prescribes as the separator does not separate: it returns the same empty output for a benign revert and for collateral damage, which is the one outcome a QA session most needs to be able to rule out about itself. What *did* settle it was accounting for every command this session ran — two `git commit` calls by pathspec and a mutation battery whose `cd` and `git checkout -- <path>` were confined to this session's own worktree — i.e. the session's own log, not the repo's. Either Step 7 names the revert/worktree-migration case and says the discriminator is your own command history rather than `git status`, or it stops promising that a fresh read distinguishes them (pointer: `skills/verify/SKILL.md` Step 7 *Take a fresh capture*, `references/CONCURRENCY.md` *The working tree is shared too*; items 0076, 0054). This still needs a row.
- 2026-09-07 — **`close`'s fourth refusal counts only `- ` bullets, so acceptance criteria written as a NUMBERED list are read as an empty section and the ticket closes with zero ticked — the exact 0044 defect the refusal exists to stop, through a door it does not cover.** The guard is `awk` `in_ac && /^- /  { bullets++ }`, and it refuses only when `ac_bullets > 0 && ac_boxes == 0`. A `1.`/`2.`/`3.` list yields `bullets = 0`, which falls through to the deliberate "an EMPTY criteria section still closes" allowance — so `close` reports success having recorded that nothing was checked, in the same words as a real close. **Six of 102 items are in this form and three of them are at `next: verify` right now**: `0075`, `0076`, `0077`, `0078`, `0081`, `0082`, all from the single capture at `b62876e`, against a template that states *"ONE form: every criterion is a `- [ ] ACn — ` checkbox"*. `0076` was rewritten to the checkbox form by the session that found this, as `verify` Step 5 requires; the other five were left alone (`CONCURRENCY.md`, *A stage writes only the ticket it holds*) and **each will close silently unticked unless its own session rewrites it first**. Two fixes, both cheap and both wanted: **(1)** count any ordered-list marker as a bullet — `/^([-*]|[0-9]+\.)/` — so the refusal fires on every non-checkbox form rather than one of them; **(2)** distinguish *absent* from *unreadable* by requiring the section to be genuinely empty, since "no criteria at all" and "criteria in a shape I cannot parse" are the two cases the current test conflates. `queue` writing the template's stated form would have prevented all six, so a guard asserting the checkbox form across `items/` belongs with it (pointer: `.claude/backlog/close` lines 211-230, `skills/queue/templates/item.md` *Acceptance criteria*, items 0075-0078, 0081, 0082; items 0076, 0044). This still needs a row.
